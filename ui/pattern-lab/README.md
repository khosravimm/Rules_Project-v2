### Pattern Lab UI

Install and run:

```bash
cd ui/pattern-lab
npm install
npm run dev
```

The app expects the FastAPI backend running on `http://localhost:8000` (override with `VITE_API_BASE_URL`).
